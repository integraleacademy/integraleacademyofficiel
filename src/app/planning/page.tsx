import { Button, Hero, SectionTitle } from '@/components/ui';
import { planning } from '@/data/site';
export const metadata={title:'Planning des formations'};
export default function Page(){return <><Hero badge="Planning" title="Planning des formations" subtitle="Une structure claire et facile à modifier pour les prochaines sessions."/><section className="mx-auto max-w-7xl px-4 py-12"><SectionTitle title="Sessions connues depuis l’inventaire"/><div className="grid gap-5 md:grid-cols-2">{planning.map(s=><div key={s.formation+s.date} className="rounded-3xl bg-white p-6 shadow-soft ring-1 ring-academy-line"><div className="text-xl font-black">{s.formation}</div><p className="mt-2 text-stone-600">{s.place}</p><p className="mt-2 font-semibold">{s.date}</p><p className="mt-2 text-sm text-green-700">{s.status}</p><div className="mt-5"><Button href="/contact">Inscription</Button></div></div>)}</div></section></>}
